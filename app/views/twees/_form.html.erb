<%= form_with(model: @twee, local: true ,url: choose_new_or_edit ) do |form| %>
  <% if twee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twee.errors.count, "error") %> prohibited this twee from being saved:</h2>

      <ul>
      <% twee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
